version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build commands required for static HTML site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []